package com.simplespace.android.lib.standard.position

class Position {

    enum class Binary {
        START, END
    }

    enum class Orientation {
        HORIZONTAL, VERTICAL
    }

    enum class ScreenMode {
        PORTRAIT, LANDSCAPE
    }

    enum class Arrangement {
        SPACE_BETWEEN, SPACE_EVENLY, CENTER, START, END
    }

}