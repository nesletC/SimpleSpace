package com.simplespace.android.lib.standard.io.file

enum class FileType {

    MP4, MP3, PDF, TXT, PNG, JPG,

    SIMPLE_ENCRYPTED_DIRECTORY,
    SIMPLE_ENCRYPTED_FILE,
    SIMPLE_MESSAGE,
    SIMPLE_NOTE,
    SIMPLE_REMINDER,

    UNKNOWN,
}